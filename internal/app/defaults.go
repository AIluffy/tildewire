package app

import "github.com/AIluffy/tildewire/internal/domain"

// DefaultStartupView is the first feed view shown when no persisted view exists.
const DefaultStartupView = domain.SourceRecommend
